/* 'use client'

import { MDXRemote } from "next-mdx-remote/rsc";


export default function MDXContent({ content, options, mdxComponents }) {

    return (
    <MDXRemote
    source={content}
    options={options}
    components={mdxComponents}
  />
    )
} */